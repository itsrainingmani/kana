[project]
name = "kana-ml"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "onnx",
    "onnxruntime",
    "onnxscript",
    "pandas",
    "pillow",
    "scikit-learn",
    "torch",
    "torchvision",
    "tqdm",
]

[tool.uv]
package = false
